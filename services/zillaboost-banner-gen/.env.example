# ── Required ─────────────────────────────────────────────────────────
# Public API base INCLUDING the /api prefix.
ODDZILLA_API_BASE=https://oddzilla.cc/api
# Must equal BANNER_GEN_TOKEN in the server's .env (openssl rand -hex 24).
BANNER_GEN_TOKEN=

# ── Local model endpoints ────────────────────────────────────────────
# LM Studio OpenAI-compatible server (prompt authoring). Auto-discovers
# the loaded model when LM_STUDIO_MODEL is unset.
LM_STUDIO_BASE_URL=http://192.168.50.37:1234
#LM_STUDIO_MODEL=

# AUTOMATIC1111/Forge-compatible image server — launch with --api.
# ComfyUI users: front it with an sdapi adapter, or swap src/imagegen.ts.
IMAGE_API_BASE=http://192.168.50.37:7860
# Optional checkpoint name as sd-webui knows it (override_settings).
#IMAGE_MODEL=

# ── Render shape (defaults suit the storefront banner strips) ────────
#IMAGE_WIDTH=1152
#IMAGE_HEIGHT=384
#IMAGE_STEPS=28
#IMAGE_CFG_SCALE=6
#IMAGE_SAMPLER=Euler a
#IMAGE_NEGATIVE_EXTRA=

# ── Cadence ──────────────────────────────────────────────────────────
# Poll interval while everything is up (ms).
#POLL_INTERVAL_MS=30000
# Backoff while the LOCAL image backend is unreachable (ms). Default 1h —
# queued jobs wait server-side and no attempts are burned meanwhile.
#BACKEND_RETRY_MS=3600000
#REQUEST_TIMEOUT_MS=60000
#IMAGE_TIMEOUT_MS=600000
#WIKIPEDIA_API_BASE=https://en.wikipedia.org/w/api.php
