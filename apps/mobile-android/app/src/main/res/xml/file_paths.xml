<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Downloaded APKs land in the app's external cache directory
         under updates/. FileProvider exposes that subfolder via the
         content:// URI handed to the install Intent. -->
    <external-cache-path
        name="apk_cache"
        path="updates/" />
</paths>
